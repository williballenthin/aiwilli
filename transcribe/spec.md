macOS app that, upon holding down a keypress (ctrl-shift-space), transcribes audio in realtime using Voxtral.c.
While key is held down, transcribes in realtime, emitting keypresses for the transcribed text.
Shows a menubar icon, while idle and different icon during transcription.
