{
  "name": "ambient-agent",
  "version": "0.1.0",
  "description": "Ambient editing companion engine: watches live edits, consolidates them in a shadow git repo, and builds rich review context. Standalone core + thin pi wrapper.",
  "type": "module",
  "bin": {
    "ambient-agent": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "cli": "bun run src/cli.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.3.14",
    "typescript": "^5.5.0"
  }
}
